export const theme = {
  colors: {
    primary: '#0094e7',
    tertiary: '#e6edf2',
    primaryAlt: '#FFF',
    border: 'rgba(0,0,0,.15)',
    text: '#000',
  },
  space: Array.from(Array(21)).map((_e, i) => `${i * 8}px`),
}
